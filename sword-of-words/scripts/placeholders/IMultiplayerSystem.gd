class_name MultiplayerSystemStub
extends Node

func Host(port: int) -> void:
    print("TODO Multiplayer host on port: %d" % port)

func Join(address: String, port: int) -> void:
    print("TODO Multiplayer join %s:%d" % [address, port])
